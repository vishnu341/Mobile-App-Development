package com.example.fitmeappfinalllimplementationv1

import android.app.IntentService
import android.content.Intent
import com.example.fitmeappfinalllimplementationv1.data.entity.History
/*

class HistoryService: IntentService("HistoryService") {
    override fun onHandleIntent(intent: Intent?) {
        val history = intent?.getParcelableExtra<History>("history")

        if (history != null) {

            val historyRepository = Graph.historyRepository
            historyRepository.insertHistory(history)
        }

    }
}*/
